<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
             xmlns:vm="clr-namespace:McpClient.ViewModels"
             x:Name="GroupList"
             x:DataType="vm:GroupListViewModel"
             x:Class="McpClient.Views.TaskGroupList">
    <Design.DataContext>
        <vm:DesignGroupList/>
    </Design.DataContext>
    <Panel Margin="20,20,0,0">
        <TextBlock Text="My Apps" FontSize="{StaticResource FontSizeLarge}" FontWeight="Bold"/>
        <TextBlock Text="管理您的智慧代理群組和工作流程" Margin="0,40,0,0"/>
        <ItemsControl Name="TaskList" ItemsSource="{Binding Groups}" Margin="0,60,0,0">
            <!-- use wrappanel as the template-->
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel Orientation="Horizontal"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <!-- data here -->
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border Classes="Card" BorderThickness="1" Height="160" Width="300" Margin="0,10,10,0">
                        <Panel>
                            <TextBlock Text="{Binding Name}" FontSize="{StaticResource FontSizeMedium}" Margin="4,4,4,0" TextWrapping="NoWrap" TextTrimming="CharacterEllipsis"/>
                            <TextBlock Text="{Binding Description}" Margin="4,32,4,0" TextWrapping="Wrap"/>
                            <Button Name="BtnDownload" Classes="Secondary" HorizontalAlignment="Left" VerticalAlignment="Bottom" Margin="0,0,0,4"
                                    Command="{Binding ElementName=GroupList, Path=DataContext.DownloadCommand}"
                                    CommandParameter="{Binding .}">Clone Agent ↓</Button>
                            <Button Name="BtnRun" Classes="Primary" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,4,4"
                                    Command="{Binding ElementName=GroupList, Path=DataContext.RunCommand}"
                                    CommandParameter="{Binding .}">Run ▶</Button>
                        </Panel>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
        <Label Name="LbEmptyList" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,80,0,0" IsVisible="False">Nothing here</Label>
    </Panel>
</UserControl>
